# Batch run ledger — 2026-07-22

Umbrella branch: `feat/batch-2026-07-22` (off `main` @ `3aee5c6`)
Baseline: 38 test files, 829 tests, all passing.

Pre-work committed to `main` before branching (unrelated in-flight tree state):
- `06123c6` feat(web): persistent storage request
- `3aee5c6` chore(docker): budkin service name + registry image

## Resolved at checkpoint (user-answered)

- **A / carousel:** widen the gesture area, keep 460px slides centered. Add
  click-drag and left/right arrow keys (both missing today).
- **B / convert:** delete the server entry AND create the server timer.
  Undo is transactional: restores the entry and discards the new timer.
- **E / timers in history:** yes; sort at their start time; tappable to open
  the timer.
- **C / nap sync:** send `nap` in the sleep body so the client wins.

## Decisions stated at checkpoint, not overridden

- **D:** store smalls-between-bigs, default 3. Global, not per-child.
  Changing X recomputes from history immediately. Also fix the cross-child
  bug at `useAppStore.ts:1939` (passes all children's entries).
- **C:** classify by START time (matches Baby Buddy). Stored per-entry, so a
  setting change affects new entries only. Insights noon-to-noon window is
  out of scope.
- **Settings layout:** D creates a new "Rhythm" group. C adds to it.
- Integration: one umbrella branch, per-item commits.

## Work items

| ID | Item | Batch | Status |
|----|------|-------|--------|
| A | Tutorial carousel gesture area on laptop web | 1 | **merged** |
| B | Edit entry to "Still ongoing" converts to a real timer | 1 | **merged** |

### B review verdict: APPROVED, merged

Diff matched the predicted file set plus `src/types/timeEntry.ts` (new optional
`startEdited?: boolean`, needed to tell a user-set start from a derived one).
Verified independently: only `setStartedAt` and `setStartedAgo` set that flag,
and they are the only two user-driven draft-start setters, so the "keep the
entry's original start" rule holds. 847 tests pass (829 + 18), tsc clean.

Deliberately broader than the convert path, per the implementer's report:
- `deleteEntry` now also scrubs the offline write queue (same latent
  resurrection bug).
- `commitWrite` deletes the orphan it created if the entry vanished mid-POST.
- The plain "start live timer" create path now carries draft metadata too,
  via the shared `buildTimerFromDraft`.
Kept, not narrowed: each is the same bug reached by a different door.

Known residue: an entry already corrupted to `end: null` by the OLD bug now
heals into a timer when opened and saved. `notes`/`tags` on a timer stay
device-local because `encodeTimerName`'s v1 grammar is a frozen wire format.

### A review verdict: APPROVED, merged

Diff stayed inside the predicted set; `_layout.tsx` untouched as required.
Verified the `consumed` flag independently: sideways wheel travel is claimed
outright, vertical only once it turns a page or is locked, so the region no
longer swallows page scrolling. Implementer drove a real headless browser at
1440x900 and checked drag-over-Next, resize, phone width and route remount.
854 tests after merge (829 + 18 + 7), tsc clean.

Batch 1 merged with no conflicts. Batches 2-4 run sequentially on the umbrella
branch directly (the prescribed mechanism for dependent items), which also
avoids the batch-1 surprise where isolated worktrees branched from `main` at
session start rather than from the umbrella branch.

### E review verdict: APPROVED (committed directly, `b7b785f`)

Commit stayed inside the predicted set; `useAppStore.ts` was not needed after
all since `openTimerEdit` already existed. Verified the union discriminant
myself: `saveAs` occurs exactly once in `types/models.ts` (:221, inside
`Timer`), so `'saveAs' in item` cannot misfire on any `Entry`. Implementer
also empirically re-checked that `detailFor` stays exhaustive by deleting a
case and confirming TS2366. 871 tests (854 + 17), tsc clean.

`groupByDay` became generic, so the Notes tab still gets `DayGroup<NoteEntry>`
and needed no edit. Timers with no `childId` count as the selected child's.

### D review verdict: APPROVED (committed directly, `902c6ac`)

Stayed inside the predicted set. Stored value counts SMALL washes between big
ones (3 = three smalls then a big), matching the user's phrasing; the settings
copy says "After every N small washes" so the off-by-one cannot creep in.
Range 1-7, chip row in a new "Rhythm" group. Verified the settings selector
reads a raw scalar, so no zustand v5 loop. 894 tests (871 + 23), tsc clean.
Implementer also drove Settings in a real browser at 390px.

Good catch not in the brief: an interval of 0 would `slice(0, 0)` to `[]`, and
`[].every()` is vacuously true, latching "big wash due" forever.
`clampSmallWashesPerBig` handles 0, negatives, fractions and non-finite.

Cross-child leak at the bath pre-select fixed as briefed.

**Deferred, NOT fixed:** the adjacent line in the same `openSheet` function,
`te.startSide = nextStartSide(get().entries)`, has the identical cross-child
leak for feeding start-side. Out of this batch's scope; report to the user.

### C review verdict: APPROVED (committed directly, `4977170`)

Stayed inside the predicted set; the optional timers-tab chip was correctly
skipped (that row picks the timer's ACTIVITY, so nap/night is a different
axis). Window stored as minutes since local midnight, so the rule survives DST
and works in the headless widget with no store. Predicate tested exhaustively
across all 1440 minutes against the old literal rule, plus wrap-around, both
boundaries and start === end. 943 tests (894 + 49), tsc clean. Implementer
verified the sheet in a real browser, including auto-seeding to Night sleep
and flipping to Nap.

**Decision the implementer made, NOT the user:** `start === end` is an EMPTY
window (nothing is a nap), the consistent reading of a half-open interval and
a useful state for a child who no longer naps. A full day is still reachable
as 00:00 to 23:59. One line plus its test to reverse. SURFACE THIS.

Behaviour change called out in the commit: sleep is classified on its START,
not its wake time. This was required to stop the value flip-flopping against
Baby Buddy on refresh.

## Run complete

All five items merged on `feat/batch-2026-07-22`. No item was BLOCKED, no
merge conflicts occurred, and the suite went 829 -> 943 with zero regressions.
| E | Ongoing timers visible in history | 2 | **merged** (direct, `b7b785f`) |
| D | Configurable big/small wash interval | 3 | **merged** (direct, `902c6ac`) |
| C | Configurable nap/night window + manual toggle | 4 | **merged** (direct, `4977170`) |

## Dependency rationale

`src/store/useAppStore.ts` (~2750 lines, one slice) is touched by B, C, D, E.
`prefs.ts` + `settings.tsx` by C and D. `selectors.ts` by D and E.
Only A is file-disjoint from everything, so only A parallelises.

Batch 1: A ‖ B → Batch 2: E → Batch 3: D → Batch 4: C
